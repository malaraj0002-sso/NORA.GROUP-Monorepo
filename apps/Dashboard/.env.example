# PostgreSQL. Server-only. Never expose to the browser.
DATABASE_URL=

# Optional: POST website revalidation after CMS saves
REVALIDATE_SECRET=
WEBSITE_REVALIDATE_URL=http://localhost:3001/api/revalidate

# Optional OpenAI-compatible translation (server-only). Drafts require human review.
TRANSLATION_API_KEY=
TRANSLATION_API_URL=
TRANSLATION_MODEL=

# Dashboard login uses PostgreSQL User + Session.
# Create the first Owner with: pnpm bootstrap:owner
# Do not use DASHBOARD_OWNER_PASSWORD. Do not use AUTH_SECRET for sessions.
